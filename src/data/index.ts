import { SiteData } from '../types';
import { eras } from './eras';
import { biographicalData } from './biographical';
import { mapData } from './maps';
import { allianceData } from './alliances';

export const siteData: SiteData = {
  eras,
  biographicalData,
  mapData,
  allianceData,
};

export default siteData;